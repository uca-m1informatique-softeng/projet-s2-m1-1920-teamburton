language: java
jdk:
 - openjdk11
 
 services:
  - docker
 
install: true
 
docker:
 - mvn install -DskipTests
 - docker build -t puerto-rico .
 
script:
 - mvn test
 - docker run puerto-rico
